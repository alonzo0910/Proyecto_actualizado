<div class="container p-4">
    <div class="row">
        <div class="col-md-4 mx-auto">
            <div class="card">
                <div class="card-body">
                    <div class="card">
                        <!--<form action="/editHouse/{{editHouse.id}}/{{editHouse.nameHouse}}/{{editHouse.direction}}" method="POST">-->
                        <form action="/editUnitSensor" method="POST">
                            <div class="form-group">
                                <!--<input type="text" class="form-control" value="{{editUnitSensor.unit}}" name="id" placeholder="id" autofocus>-->
                                <p>Unidad Actual: {{editUnitSensor.unit}}</p>
                            </div>
                            <!--<div class="form-group">
                                <select name="newUnit">
                                    <option value="volvo">Centigrados</option>
                                    <option value="saab">Kelvin</option>
                                    <option value="opel">Fahrenheit</option>
                                </select>
                            </div>-->
                            <select class="selectpicker" data-style="btn-info" name="newUnit">
                                <optgroup>
                                    <option name="unit1" value="Centigrados">Centigrados</option>
                                    <option name="unit2" value="Kelvin">Kelvin</option>
                                    <option name="unit3" value="Fahrenheit">Fahrenheit</option>
                                </optgroup>
                            </select>
                            <div class="form-group" style="display : none">
                                <input type="text" class="form-control" value="{{editUnitSensor.id}}" name="id" placeholder="id" autofocus>
                            </div>
                            <div class="form-group">
                                <button class="btn btn-success btn-block">
                                    <b>Guardar</b>
                                </button>
                            </div>
                        </form>
                </div>
            </div>
        </div>
    </div>
</div>